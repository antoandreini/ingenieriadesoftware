<header>
  <nav class="navbar navbar-default">
    <div class="container-fluid">
      <!-- Brand and toggle get grouped for better mobile display -->
      <div class="navbar-header">
        <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
          <span class="sr-only">Toggle navigation</span>
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
        </button>
        <a class="thumbnail" href="#">
        <%= image_tag "logo.png", title: "Logo", class: "img-responsive" %></a>
      </div>
      <!-- Collect the nav links, forms, and other content for toggling -->
      <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
        <ul class="nav navbar-nav">  <% if user_signed_in? %>
            <li><p class = "navbar-text">Bienvenido/a <strong><%= current_user.email %></strong>.</p></li>
            <li><a href="#"><span class="glyphicon glyphicon-comment" aria-hidden="true"></span> Nueva gauchada</a></li>
              <li class="dropdown">
              <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false"><span class="glyphicon glyphicon-collapse-down" aria-hidden="true"></span> Opciones <span class="caret"></span></a>
              <ul class="dropdown-menu">
                <li><a href="#"><span class="glyphicon glyphicon-eye-open" aria-hidden="true"></span> Ver perfil</a></li>
                <li><a href="#"><span class="glyphicon glyphicon-cog" aria-hidden="true"></span><%= link_to 'Configuración', edit_user_registration_path, :class => 'navbar-link' %> </a></li>
                <li><a href="#"><span class="glyphicon glyphicon-usd" aria-hidden="true"></span>Comprar puntos</a></li>
                <li><a href="#"><span class="glyphicon glyphicon-off" aria-hidden="true"></span><%= link_to "Cerrar sesión", destroy_user_session_path, method: :delete, :class => 'navbar-link'  %></a></li>
                <% if user_signed_in? and current_user.administrador == true %>
                 <li> <a href="/logros"> <span class="glyphicon glyphicon-eye-open" aria-hidden="true"></span> Ver Logros </a> </li>
                <% end %>
              </ul>
            </li>


          <% else %>
            <li><button type="submit" class="btn"><span class="glyphicon glyphicon-user" aria-hidden="true"></span>   <%= link_to "Iniciar sesión", new_user_session_path, :class => 'navbar-link'  %></button></li>
            <li><button type="submit" class="btn btn-warning"><span class="glyphicon glyphicon-thumbs-up" aria-hidden="true"></span><%= link_to "¿No tenés cuenta? ¡Registrate!", new_user_registration_path, :class => 'navbar-link'  %> </button></li>
          <% end %>


        </ul>
        <form class="navbar-form navbar-left">
          <div class="form-group">
            <input type="text" class="form-control" placeholder="Buscar gauchadas...">
          </div>
          <button type="submit" class="btn btn-warning"><span class="glyphicon glyphicon-search" aria-hidden="true"></span> Buscar</button>
        </form>
        <ul class="nav navbar-nav navbar-right">
           <% if user_signed_in? and current_user.administrador == true %>
          <li class="dropdown">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false"><span class="glyphicon glyphicon-collapse-down" aria-hidden="true"></span> Administrador <span class="caret"></span></a>
            <ul class="dropdown-menu">
              <li><a href="#"><span class="glyphicon glyphicon-star" aria-hidden="true"></span> Ver Listado de logros</a></li>
              <li><a href="#"><span class="glyphicon glyphicon-piggy-bank" aria-hidden="true"></span> Ver reporte de ingresos</a></li>
              <li><a href="#"><span class="glyphicon glyphicon-list" aria-hidden="true"></span> Ver ranking de usuarios</a></li>
            </ul>
          </li>
            <% end %>
          <li><button type="button" class="btn btn-warning"><span class="glyphicon glyphicon-question-sign" aria-hidden="true"></span> Ayuda </button>
          </li>
            </ul>
          </li>
        </ul>
      </div><!-- /.navbar-collapse -->
    </div><!-- /.container-fluid -->
  </nav>
  </header>
